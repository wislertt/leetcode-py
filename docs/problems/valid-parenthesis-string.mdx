---
title: "Valid Parenthesis String Python Solution"
description: "Tested Python solution for LeetCode 678 with 14 pytest cases. Generate a practice environment with lcpy."
---

{/* GENERATED by scripts/gen_problems.py - do not edit */}

LeetCode 678, Medium. Topics: String, Dynamic Programming, Stack, Greedy. [View on LeetCode](https://leetcode.com/problems/valid-parenthesis-string/description/).

Generate this problem as a practice environment: tested reference solution, 14 [parametrized pytest cases](/practice/testing), and a playground notebook:

```bash
lcpy gen -n 678   # by problem number
lcpy gen -s valid_parenthesis_string   # by problem name
```

## Problem

Given a string `s` containing only three types of characters: `'('`, `')'` and `'*'`, return `true` *if* `s` *is **valid***.

The following rules define a **valid** string:

- Any left parenthesis `'('` must have a corresponding right parenthesis `')'`.
- Any right parenthesis `')'` must have a corresponding left parenthesis `'('`.
- Left parenthesis `'('` must go before the corresponding right parenthesis `')'`.
- `'*'` could be treated as a single right parenthesis `')'` or a single left parenthesis `'('` or an empty string `""`.

### Examples

```
Input: s = "()"
Output: true
```

```
Input: s = "(*)"
Output: true
```

```
Input: s = "(*))"
Output: true
```

### Constraints

- 1 &lt;= s.length &lt;= 100
- `s[i]` is `'('`, `')'` or `'*'`.

## Solution

Reference implementation from [solution.py on GitHub](https://github.com/wislertt/leetcode-py/blob/main/leetcode/valid_parenthesis_string/solution.py), full suite in [test_solution.py](https://github.com/wislertt/leetcode-py/blob/main/leetcode/valid_parenthesis_string/test_solution.py):

```python
class Solution:
    # Time: O(n)
    # Space: O(1)
    def check_valid_string(self, s: str) -> bool:
        # Range of possible open-parenthesis counts after processing each char.
        low = 0
        high = 0
        for char in s:
            if char == "(":
                low += 1
                high += 1
            elif char == ")":
                low = max(low - 1, 0)
                high -= 1
            else:  # '*' can act as '(', ')' or empty
                low = max(low - 1, 0)
                high += 1
            if high < 0:
                return False
        return low == 0
```

## Complexity

| Time | Space |
| --- | --- |
| O(n) | O(1) |

## Tags

[NeetCode 150](/catalog/neetcode-150), [NeetCode 250](/catalog/neetcode-250), [NeetCode All](/catalog/neetcode).
