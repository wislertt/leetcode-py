---
title: "Maximum Average Subarray I Python Solution"
description: "Tested Python solution for LeetCode 643 with 18 pytest cases. Generate a practice environment with lcpy."
---

{/* GENERATED by scripts/gen_problems.py - do not edit */}

LeetCode 643, Easy. Topics: Array, Sliding Window. [View on LeetCode](https://leetcode.com/problems/maximum-average-subarray-i/description/).

Generate this problem as a practice environment: tested reference solution, 18 [parametrized pytest cases](/practice/testing), and a playground notebook:

```bash
lcpy gen -n 643   # by problem number
lcpy gen -s maximum_average_subarray_i   # by problem name
```

## Problem

You are given an integer array `nums` consisting of `n` elements, and an integer `k`.

Find a contiguous subarray whose **length is equal to** `k` that has the maximum average value and return *this value*. Any answer with a calculation error less than `10^-5` will be accepted.

### Examples

```
Input: nums = [1,12,-5,-6,50,3], k = 4
Output: 12.75000
Explanation: Maximum average is (12 - 5 - 6 + 50) / 4 = 51 / 4 = 12.75
```

```
Input: nums = [5], k = 1
Output: 5.00000
```

### Constraints

- n == nums.length
- 1 &lt;= k &lt;= n &lt;= 10^5
- -10^4 &lt;= nums[i] &lt;= 10^4

## Solution

Reference implementation from [solution.py on GitHub](https://github.com/wislertt/leetcode-py/blob/main/leetcode/maximum_average_subarray_i/solution.py), full suite in [test_solution.py](https://github.com/wislertt/leetcode-py/blob/main/leetcode/maximum_average_subarray_i/test_solution.py):

```python
class Solution:
    # Time: O(n)
    # Space: O(1)
    def find_max_average(self, nums: list[int], k: int) -> float:
        window = sum(nums[:k])
        best = window
        for i in range(k, len(nums)):
            window += nums[i] - nums[i - k]
            if window > best:
                best = window
        return best / k
```

## Complexity

| Time | Space |
| --- | --- |
| O(n) | O(1) |

## Tags
