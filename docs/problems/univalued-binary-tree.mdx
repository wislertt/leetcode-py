---
title: "Univalued Binary Tree Python Solution"
description: "Tested Python solution for LeetCode 965 with 20 pytest cases. Generate a practice environment with lcpy."
---

{/* GENERATED by scripts/gen_problems.py - do not edit */}

LeetCode 965, Easy. Topics: Tree, Depth-First Search, Breadth-First Search, Binary Tree. [View on LeetCode](https://leetcode.com/problems/univalued-binary-tree/description/).

Generate this problem as a practice environment: tested reference solution, 20 [parametrized pytest cases](/practice/testing), and a playground notebook:

```bash
lcpy gen -n 965   # by problem number
lcpy gen -s univalued_binary_tree   # by problem name
```

## Problem

A binary tree is &lt;strong>uni-valued&lt;/strong> if every node in the tree has the same value.

Given the &lt;code>root&lt;/code> of a binary tree, return &lt;code>true&lt;/code> if the given tree is &lt;strong>uni-valued&lt;/strong>, or &lt;code>false&lt;/code> otherwise.

### Examples

![Example 1](https://assets.leetcode.com/uploads/2018/12/28/unival_bst_1.png)

```
Input: root = [1,1,1,1,1,null,1]
Output: true
```

![Example 2](https://assets.leetcode.com/uploads/2018/12/28/unival_bst_2.png)

```
Input: root = [2,2,2,5,2]
Output: false
```

### Constraints

- The number of nodes in the tree is in the range [1, 100]
- 0 &lt;= Node.val &lt; 100

## Solution

Reference implementation from [solution.py on GitHub](https://github.com/wislertt/leetcode-py/blob/main/leetcode/univalued_binary_tree/solution.py), full suite in [test_solution.py](https://github.com/wislertt/leetcode-py/blob/main/leetcode/univalued_binary_tree/test_solution.py):

```python
from leetcode_py import TreeNode


class Solution:
    # Time: O(n)
    # Space: O(h)
    def is_unival_tree(self, root: TreeNode[int] | None) -> bool:
        if root is None:
            return True
        stack = [root]
        target = root.val
        while stack:
            node = stack.pop()
            if node.val != target:
                return False
            if node.left is not None:
                stack.append(node.left)
            if node.right is not None:
                stack.append(node.right)
        return True
```

## Complexity

| Time | Space |
| --- | --- |
| O(n) | O(h) |

## Tags
