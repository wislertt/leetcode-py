---
title: "Peak Index in a Mountain Array Python Solution"
description: "Tested Python solution for LeetCode 852 with 18 pytest cases. Generate a practice environment with lcpy."
---

{/* GENERATED by scripts/gen_problems.py - do not edit */}

LeetCode 852, Medium. Topics: Array, Binary Search, Ternary Search. [View on LeetCode](https://leetcode.com/problems/peak-index-in-a-mountain-array/description/).

Generate this problem as a practice environment: tested reference solution, 18 [parametrized pytest cases](/practice/testing), and a playground notebook:

```bash
lcpy gen -n 852   # by problem number
lcpy gen -s peak_index_in_a_mountain_array   # by problem name
```

## Problem

You are given an integer **mountain** array `arr` of length `n` where the values increase to a **peak element** and then decrease.

Return the index of the peak element.

Your task is to solve it in `O(log(n))` time complexity.

### Examples

```
Input: arr = [0,1,0]
Output: 1
```

```
Input: arr = [0,2,1,0]
Output: 1
```

```
Input: arr = [0,10,5,2]
Output: 1
```

### Constraints

- 3 &lt;= arr.length &lt;= 10^5
- 0 &lt;= arr[i] &lt;= 10^6
- arr is guaranteed to be a mountain array.

## Solution

Reference implementation from [solution.py on GitHub](https://github.com/wislertt/leetcode-py/blob/main/leetcode/peak_index_in_a_mountain_array/solution.py), full suite in [test_solution.py](https://github.com/wislertt/leetcode-py/blob/main/leetcode/peak_index_in_a_mountain_array/test_solution.py):

```python
class Solution:
    # Time: O(log n)
    # Space: O(1)
    def peak_index_in_mountain_array(self, arr: list[int]) -> int:
        left, right = 0, len(arr) - 1
        while left < right:
            mid = (left + right) // 2
            if arr[mid] < arr[mid + 1]:
                left = mid + 1
            else:
                right = mid
        return left
```

## Complexity

| Time | Space |
| --- | --- |
| O(log n) | O(1) |

## Tags
